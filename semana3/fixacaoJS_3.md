<!-- function calculaNota(ex, p1, p2) {
let total = (ex _ 1 + p1 _ 2 + p2 \* 3)
let totalMedia = total / 6

    if(totalMedia >= 9){
      return ("A");
     }else if(totalMedia < 9 && totalMedia >= 7.5){
      return ("B");
         }else if(totalMedia < 7.5 && totalMedia >= 6 ){
             return("C");
             }else if (totalMedia < 6){
               return ("D");
             }

return totalMedia
} -->
